paralogueTravant1
    .byte Right_slot
    .text "In entrusting this spear to you, the keys to"
    .byte NewLine
    .text "Thracia's future lay in your hands, Arion."

    .byte ScrollText
    .text "What happens next is at your discretion."
    .byte NewLine
    .text "All I ask of you is one thing:"

    .byte ScrollText
    .text "See that his people are liberated from"
    .byte NewLine
    .text "their suffering."
    .byte NewLine
    .text "Let our people be tormented no longer."

    .byte ScrollText
    .text "That is my only wish."

    .byte ScrollText
    .text "Your Father,"
    .byte NewLine
    .text "The Former King of Thracia."
    .byte WaitForA

    .byte EndText
    
paralogueTravant2
    .byte Right_slot
    .word LoadPortrait
    .word Travant_Portrait
    .text "..."
    .byte WaitForA

    .byte EndText

paralogueTravant3
    .byte Right_slot
    .word LoadPortrait
    .word Travant_Portrait
    .text "...Reveal yourselves."
    .byte WaitForA

    .byte EndText

paralogueVeld
    .byte Right_slot
    .word LoadPortrait
    .word Veld_Portrait
    .text "Eh heh heh..."
    .byte WaitForA

    .byte EndText

paralogueManfroy
    .byte Right_slot
    .word LoadPortrait
    .word manfroy_portrait
    .text "Hmm, so Raydrik has made it to Leonster, has he?"
    .byte WaitForA

    .byte Left_slot
    .word LoadPortrait
    .word Veld_Portrait
    .text "Reportedly so, Your Eminence..."
    .byte WaitForA

    .byte Right_Slot
    .word LoadPortrait
    .word Travant_portrait
    .byte ScrollText
    .text "Remind me why I came all this way in the middle"
    .byte NewLine
    .text "of the night?"
    .byte WaitForA

    .byte Left_Slot
    .byte ScrollText
    .word LoadPortrait
    .word manfroy_portrait
    .text "King Travant..."
    .byte NewLine
    .text "The battlefield is ever-changing."
    .byte WaitForA

    .byte ScrollText
    .text "Kalf is no longer your primary target..."
    .byte NewLine
    .text "A new problem has arisen: Prince Quan."
    .byte WaitForA

    .byte ScrollText
    .text "With him being wed to the Chalphy princess,"
    .byte NewLine
    .text "it is only a matter of time before an heir to"
    .byte NewLine
    .text "Gae Bolg is sired..."
    .byte WaitForA

    .byte Right_Slot
    .word LoadPortrait
    .word Veld_Portrait
    .byte ScrollText
    .text "Perhaps an heir who can wield both Gae Bolg"
    .byte NewLine
    .text "AND Tyrfing. A quarrel we do not wish to"
    .byte NewLine
    .text "deal with, surely?"
    .byte WaitForA

    .byte Left_Slot
    .word LoadPortrait
    .word Travant_Portrait
    .byte ScrollText
    .text "..."
    .byte NewLine
    .text "So, when does this happen?"
    .byte WaitForA

    .byte Right_Slot
    .word LoadPortrait
    .word manfroy_portrait
    .byte ScrollText
    .text "Good fortune come to those who wait,"
    .byte NewLine
    .text "Your Majesty..."
    .byte NewLine
    .text "This is not something to be done hastely."
    .byte WaitForA

    .byte ScrollText
    .text "All in due time..."
    .byte WaitForA

    .byte Left_Slot
    .byte ScrollText
    .text "..."
    .byte NewLine
    .text "What about that blasted Raydrik?"
    .byte NewLine
    .text "Is he truly necessary?"
    .byte WaitForA

    .byte Right_Slot
    .byte ScrollText
    .text "That brute is only a small part of Bishop Veld's"
    .byte NewLine
    .text "brilliant plan to hand Thracia to its rightful ruler."
    .byte WaitForA

    .byte Left_Slot
    .byte ScrollText
    .text "I'm listening."
    .byte WaitForA

    .byte Right_Slot
    .word LoadPortrait
    .word Veld_Portrait
    .byte ScrollText
    .text "Upon returning from a night of celebrations,"
    .byte NewLine
    .text "King Auerbach will meet his end. Perhaps, one "
    .byte NewLine
    .text "drink too many, for such a fragile body..."
    .byte WaitForA

    .byte ScrollText
    .text "It is only natural that Prince Carl, the King's"
    .byte NewLine
    .text "child, should succeed him."
    .byte WaitForA

    .byte ScrollText
    .text "But, a boy so young is cannot be ready to"
    .byte NewLine
    .text "rule a kingdom..."
    .byte WaitForA

    .byte ScrollText
    .text "The boy will seek for guidance, naturally."
    .byte NewLine
    .text "And who better to listen to than his personal"
    .byte NewLine
    .text "guard, Sir Raydrik?"
    .byte WaitForA

    .byte Left_Slot
    .byte ScrollText
    .text "Surely even a child such as Carl can see Raydrik"
    .byte NewLine
    .text "for what he truly is: little more than "
    .byte NewLine
    .text "a schoolyard bully."
    .byte WaitForA

    .byte Right_Slot
    .word LoadPortrait
    .word manfroy_portrait
    .byte ScrollText
    .text "The child won't be needed for long..."
    .byte NewLine
    .text "And with no heir to inherit Connaught's throne,"
    .byte NewLine
    .text "perhaps, military rule becomes necessary?"
    .byte WaitForA

    .byte ScrollText
    .text "Raydrik will create chaos upon an unclaimed crown."
    .byte WaitForA

    .byte Left_Slot
    .byte ScrollText
    .text "...You mean to tell me you plan on giving that"
    .byte NewLine
    .text "ape the power to rule a kingdom?"
    .byte WaitForA

    .byte ScrollText
    .text "I still fail to see how this benefits my people."
    .byte WaitForA

    .byte Right_Slot
    .byte ScrollText
    .text "Need we spell it out for you, Your Majesty?"
    .byte NewLine
    .text "If it weren't for Gae Bolg and the Lanzenritter,"
    .byte NewLine
    .text "Connaught, not Leonster, would be the strongest."
    .byte WaitForA

    .byte ScrollText
    .text "Next time Munster needs to band together,"
    .byte NewLine
    .text "half their might will be led by a raging lunatic!"
    .byte WaitForA

    .byte ScrollText
    .text "Their army will have been crippled before a battle"
    .byte NewLine
    .text "has even begun!"
    .byte WaitForA
    
    .byte Left_Slot
    .byte ScrollText
    .text "...I see."
    .byte NewLine
    .text "I have waited many years for the day my people"
    .byte NewLine
    .text "could reunite with their kind, I can wait more."
    .byte WaitForA

    .byte ScrollText
    .text "Is that all?"
    .byte WaitForA

    .byte Right_Slot
    .byte ScrollText
    .text "For now."
    .byte WaitForA

    .byte Left_Slot
    .byte ScrollText
    .text "..."
    .byte WaitForA

    .byte EndText

paralogueManfroyContinuation

    .byte Right_slot
    .word LoadPortrait
    .word Travant_portrait
    .text "...Why are you telling me all of this?"
    .byte WaitForA

    .byte Left_slot
    .word LoadPortrait
    .word manfroy_portrait
    .text "You are an ambitious man, King Travant."
    .byte NewLine
    .text "We recognize your desire for a united Thracia."
    .byte WaitForA

    .byte Right_slot
    .byte ScrollText
    .text "Spare me the preamble. You don't offer such"
    .byte NewLine
    .text "tidings without expecting something in return."
    .byte WaitForA

    .byte Left_slot
    .byte ScrollText
    .text "A perceptive observation."
    .byte NewLine
    .text "Indeed, there is a mutual benefit to be found."
    .byte WaitForA

    .byte ScrollText
    .text "Once Connaught falls into disarray, the Loptyrian"
    .byte NewLine
    .text "Empire will have no interest in Thracia."
    .byte WaitForA

    .byte ScrollText
    .text "You will rule the highlands unchallenged, free"
    .byte NewLine
    .text "to pursue your dreams of unification."
    .byte WaitForA

    .byte Right_slot
    .byte ScrollText
    .text "And in exchange? What 'loyalty' are you asking"
    .byte NewLine
    .text "for? Your kind never offers without taking"
    .byte NewLine
    .text "twice as much."
    .byte WaitForA

    .byte Left_slot
    .byte ScrollText
    .text "You are wise to tread carefully, Your Majesty."
    .byte NewLine
    .text "But rest assured, our intentions are simple."
    .byte WaitForA

    .byte ScrollText
    .text "We require your cooperation in crippling "
    .byte NewLine
    .text "Munster's strength, paving the way for"
    .byte NewLine
    .text "our influence."
    .byte WaitForA

    .byte Right_slot
    .byte ScrollText
    .text "Hah. Influence, is it? Let's call it what it is:"
    .byte NewLine
    .text "domination. Don't think for a moment I've missed"
    .byte NewLine
    .text "your endgame here."
    .byte WaitForA

    .byte Left_slot
    .byte ScrollText
    .text "And yet, you stand to gain more than you lose."
    .byte NewLine
    .text "Your kingdom, united. Your enemies, divided."
    .byte WaitForA

    .byte ScrollText
    .text "All we ask is that Thracia lends its strength now,"
    .byte NewLine
    .text "to ensure a future where it reigns supreme."
    .byte WaitForA

    .byte Right_slot
    .byte ScrollText
    .text "No. All you ask is that I play pawn to your"
    .byte NewLine
    .text "schemes. But a pawn that survives becomes"
    .byte NewLine
    .text "a king."
    .byte WaitForA

    .byte ScrollText
    .text "I'll agree to your terms for now, Manfroy. But"
    .byte NewLine
    .text "make no mistake: I serve only Thracia."
    .byte WaitForA

    .byte Left_slot
    .byte ScrollText
    .text "A reasonable stance. We shall see how this"
    .byte NewLine
    .text "partnership unfolds."
    .byte WaitForA

    .byte Right_slot
    .byte ScrollText
    .text "Hmph. And if your empire oversteps? Then I'll"
    .byte NewLine
    .text "show you why the highlands were never conquered."
    .byte WaitForA

    .byte Left_slot
    .byte ScrollText
    .text "We expect nothing less from you, King Travant."
    .byte WaitForA

    .byte EndText

    
paralogueManfroy2
    .byte Right_slot
    .word LoadPortrait
    .word manfroy_portrait
    .text "Say what you will, the man has heart."
    .byte WaitForA
   
    .byte EndText

